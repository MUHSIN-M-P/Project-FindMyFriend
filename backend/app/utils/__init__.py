# Utility functions and helpers
# Without __init__.py, Python won't recognize the directory as a package

# This allows you to import modules from the package using dot notation:

# from app.models.user import User  # Instead of using full path